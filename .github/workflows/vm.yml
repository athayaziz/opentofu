name: "Proxmox GitOps: Deploy VM"

on:
  push:
    branches:
      - main
    paths:
      - 'vms/**'
      - '.github/workflows/vm.yml'
  workflow_dispatch:

jobs:
  deploy_vm:
    name: "Provision VM & Run Ansible"
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Step 1: Eksekusi OpenTofu VM
      - name: OpenTofu Init & Apply (VM)
        working-directory: ./vms/tofu
        run: |
          tofu init -backend-config="path=/home/runner/vm-terraform.tfstate"
          tofu apply -auto-approve \
            -var="proxmox_endpoint=${{ secrets.PROXMOX_ENDPOINT }}" \
            -var="proxmox_api_token=${{ secrets.PROXMOX_API_TOKEN }}" \
            -var="ssh_public_key=${{ secrets.SSH_PUBLIC_KEY }}"

      # Step 2: Siapkan SSH Key untuk Ansible
      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ansible
          chmod 600 ~/.ssh/id_ansible

      # Step 3: Eksekusi Ansible
      - name: Run Ansible Playbook
        working-directory: ./vms/ansible
        run: |
          ansible-playbook --private-key ~/.ssh/id_ansible playbooks/site.yml
